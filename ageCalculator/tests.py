from django.test import TestCase

# Create your tests here.

# tests for age calculator
# - if dob is less than one month ago, values of year and month should be 0
# - if dob is less than one year ago, values of year should be 0
# - if dob is more than one year ago, values of year should greater than 0
